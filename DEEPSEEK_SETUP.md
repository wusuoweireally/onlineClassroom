# DeepSeek AI 配置说明

## 概述

本系统支持使用 DeepSeek AI 进行智能问题分类。如果您想使用 AI 分类功能，需要先配置 DeepSeek API 密钥。

## 配置步骤

### 1. 获取 DeepSeek API 密钥

1. 访问 [DeepSeek 平台](https://platform.deepseek.com/)
2. 注册账号并登录
3. 进入 API 管理页面
4. 创建新的 API 密钥
5. 复制生成的 API 密钥

### 2. 配置环境变量

1. 在项目根目录创建 `.env` 文件：

   ```bash
   cp .env.example .env
   ```

2. 编辑 `.env` 文件，添加您的 DeepSeek API 密钥：

   ```bash
   DEEPSEEK_API_KEY=你的密钥
   ```

3. 重启后端服务器：
   ```bash
   # 停止当前服务器（Ctrl+C）
   # 然后重新启动
   cd server
   node index.js
   ```

### 3. 验证配置

1. 登录教师管理界面
2. 点击 "检查 AI 状态" 按钮
3. 如果配置正确，应该显示 "AI 状态检查成功" 消息
4. 状态指示器应该显示绿色圆点

## 功能说明

### AI 分类功能

- **启用时**：使用 DeepSeek AI 进行智能分类，准确度更高
- **禁用时**：使用关键词匹配进行分类，无需 API 密钥

### 状态指示器

- 🟢 **绿色**：DeepSeek API 连接正常
- 🔴 **红色**：API 密钥未配置或连接失败
- ⚪ **灰色**：状态未知

### 自动切换

如果 DeepSeek API 不可用，系统会自动切换到关键词分类模式，确保功能正常使用。

## 故障排除

### 常见问题

1. **API 密钥无效**

   - 检查密钥是否正确复制
   - 确认密钥没有过期
   - 检查账户余额

2. **网络连接失败**

   - 检查网络连接
   - 确认防火墙设置
   - 尝试重启服务器

3. **权限不足**
   - 确认使用教师或管理员账号登录
   - 检查 JWT token 是否有效

## 注意事项

- DeepSeek API 调用可能产生费用，请根据实际需求使用
- 建议定期检查 API 额度和使用情况
- 生产环境中请妥善保管 API 密钥，不要提交到版本控制系统
